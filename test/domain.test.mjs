import { describe, expect, it } from 'vitest'; import { createComplianceRegistry } from '../domain.mjs'; const engineer={id:'e',role:'compliance-engineer'},manager={id:'m',role:'supplier-manager'},governor={id:'g',role:'compliance-governor'},input={id:'COMP-378',supplier:'supplier',standard:'ISO-27001'},evidence='The supplier control evidence was validated against the agreed requirements and maintained review procedure.';const code=t=>{try{t()}catch(e){return e.code}};describe('compliance governance',()=>{it('defines attests approves and opens exception',()=>{const r=createComplianceRegistry();r.define(engineer,input);r.attest(manager,input.id,evidence);r.approve(governor,input.id);expect(r.exception(governor,input.id,'Corrective action required.').state).toBe('exception')});it('rejects invalid and duplicate policies',()=>{const r=createComplianceRegistry();expect(code(()=>r.define(engineer,{...input,id:'bad'}))).toBe('VALIDATION');r.define(engineer,input);expect(code(()=>r.define(engineer,input))).toBe('CONFLICT')});it('enforces roles, missing policy, evidence, and lifecycle gates',()=>{const r=createComplianceRegistry();r.define(engineer,input);expect(code(()=>r.attest(engineer,input.id,evidence))).toBe('FORBIDDEN');expect(code(()=>r.attest(manager,'COMP-404',evidence))).toBe('NOT_FOUND');expect(code(()=>r.attest(manager,input.id,'brief'))).toBe('VALIDATION');expect(code(()=>r.approve(governor,input.id))).toBe('CONFLICT')})});

